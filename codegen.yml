overwrite: true
schema:
  - http://localhost:3000/graphql
documents:
  - "./graphql/**/*.ts"
generates:
  ./types.generated.ts:
    config:
      avoidOptionals: true
      maybeValue: "T"
      skipTypename: true
      strictScalars: true
      scalars:
        DateTime: string
        Dimension: number
        HexColor: string
        JSON: any
        Quality: number
    plugins:
      - "typescript"
      - "typescript-operations"
